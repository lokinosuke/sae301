<style>

    nav {
        width: 100%;
    }

    li {
        margin: auto;
        margin-right: 20px;
    }

    .nav-link {
        font-family: Arial;
        font-weight: bold;
    }

    .nav-item {
        padding: 5px;
    }

    .badge {
        margin-left: 5px;
        background-color: #784F86;
    }

    .navbar {
        padding: 20px 20px 20px 40px;
        background: rgb(0,0,0);
        background: linear-gradient(180deg, rgba(0,0,0,0.3292716744901085) 0%, rgba(255,255,255,0) 100%);
    }

    .btn-nav {
        border-radius: 50px;
        background-color: black;
    }

    .btn-nav:hover {
        background-color: black;
    }

    .dropbtn {
        display: flex;
        align-items: center;
        justify-content: space-around;
        border: none;
        border-radius: 50px;
    }

    .dropdown .dropbtn {
        font-size: 16px;
        outline: none;
        color: white;
        padding: 7px;
        background-color: black;
        font-family: inherit;
        margin: 0;
        border-radius: 25px;
        width: 180px;
    }

    .dropdown {
        align-items: center;
        padding: 5px;
        margin-right: 20px;
        justify-content: flex-end;
    }

    .dropdown-content {
        display: none;
        position: absolute;
        background-color: #f9f9f9;
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        z-index: 1;
        margin-top: 5px;
    }

    .dropdown-content a {
        float: none;
        color: black;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        text-align: center;
    }

    .dropdown:hover .dropdown-content {
        display: block;
    }

    .btn-nav:active {
        background-color: black;
        color: black;
    }

    #logo {
        width: 130px;
    }


</style>


<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand text-black" href="{{ path('app_accueil') }}"><img id="logo" src="{{ asset('assets/images/logo_black.png') }}" alt="Logo"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link text-black" href="{{ path('app_recherche') }}">Recherche</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-black" href="{{ path('app_lieu') }}">Salles</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-black" href="{{ path('app_affichage') }}">Billeterie</a>
            </li>
            <li class="nav-item">
                <button class="btn btn-nav" type="button"><a class="nav-link text-white" href="{{ path('app_panier') }}">MON PANIER<span id="panier" class="badge badge-pill">0</span></a>
                </button>
            </li>
            {% if app.user %}
                <div class="dropdown">
                    <button class="dropbtn"><a class="nav-link text-white">{{ app.user.userIdentifier }}</a>
                        <i class="fa fa-caret-down"></i>
                    </button>
                    <div class="dropdown-content">
                        <a href="{{ path('app_profile') }}">Mon compte</a>
                        <a class="nav-link text-bg-danger" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </div>
                </div>
            {% else %}
                <li class="nav-item">
                    <button class="btn btn-nav" type="button"><a class="nav-link text-white" href="{{ path('app_login') }}">Se connecter</a></button>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>

